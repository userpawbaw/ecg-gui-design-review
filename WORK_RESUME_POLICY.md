# 이 Work의 중단 복구·예약 실행 규칙

버전 1.0 · 2026-09-13 · 사용자가 요청한 지속 지침. 수동 작업과 예약 실행에 모두 적용한다.

## 1. 시작할 때 읽을 것

현재 채팅의 최신 사용자 지시 → `AGENTS.md` → `WORK_STATE.json` → `PLAN.md`의 현재 상태 → `WORKLOG.md`의 해당 단계 증거를 읽는다. GUI 수정에는 `docs/21_ui_refinement_workflow_final.md`도 적용한다. 최신 사용자 지시가 과거 문서보다 우선하며, 문서의 제안·과거 승인 대기를 현재 작업 승인으로 오해하지 않는다.

지속 저장소는 https://github.com/userpawbaw/ecg-gui-design-review 이다. 로컬 경로는 세션마다 바뀔 수 있다. 원격 main의 SHA와 관련 파일을 조회하고 로컬 변경을 비교한다. 로컬이 오래됐다는 이유로 미커밋 사용자 변경을 덮어쓰지 말고 별도로 보존한다. 채팅의 “완료”보다 파일·로그·테스트·원격 커밋을 우선한다. 문서만 바뀌었다면 대용량 데이터 전체를 다시 생성하지 않는다.

## 2. 첫 판단: A / B / C

**A — 다른 실행이 실제 진행 중:** 동일 Work와 과업을 수행하는 실행의 식별자, 실행 도구 상태, 프로세스 시작 시각, 최근 로그·산출물 진행을 가능한 범위에서 확인한다. 정상 실행이면 상태만 보고하고 종료한다. 파일 수정, 중복 실험·학습·빌드·테스트, 별도 예약 생성 모두 하지 않는다.

**B — 원래 승인된 계획 전체 완료:** 계획의 완료 조건과 해당 버전의 산출물·검증 기록을 가볍게 대조한다. 모두 완료면 상태만 보고하고 종료한다. 완료 결과를 다시 만들거나 무변경 확인만을 위해 커밋하지 않는다. 현재 요청 완료와 전체 GUI/장치 프로젝트 완료는 별개다.

**C — 중단·미완료:** 원래 계획, 마지막 검증된 체크포인트, 중단 단계, 실제 변경분, 결과·테스트 종료 상태, 미시작 단계를 확인한다. 신뢰할 수 있는 완료 결과는 유지하고 다음 단계부터 이어 간다. 일부 수정·불완전 출력·종료 불명·검증 불가이면 그 단계의 안전한 시작점부터 복구한다. 불완전한 결과에 의존한 후속 단계도 검증하고 필요한 것만 다시 실행한다. 재개한 한 단계에서 멈추지 말고 승인된 남은 작업을 계속 진행한다.

미완료지만 필수 자료·권한·사용자 결정·실기 검증이 없으면 `blocked` 또는 `awaiting_user`로 남기고 이유와 재개 조건만 보고한다. 이를 전체 완료로 처리하거나 차단된 실행 경로를 우회하지 않는다. 사용자가 중지·취소·보류한 작업을 예약으로 재개하지 않는다.

## 3. 중복 방지와 한계

- `in_progress` 문자열, PID 하나, 오래된 lock 파일, 최근 커밋 하나만으로 실행 생존을 판정하지 않는다. 실행 식별자와 진행 증거를 함께 본다. 프로세스 종료와 완성된 결과도 대조한다.
- 실행 소유자는 과업 ID, 실행 ID/환경, 시작 시각, 현재 단계, 최근 진척 증거를 `WORK_STATE.json`의 `execution`에 남긴다. 장기 명령은 도구 session/job ID와 로그 위치를 기록하고 주요 단계·출력 수신 때 갱신한다. 의미 없는 heartbeat를 원격에 계속 커밋하지 않는다.
- 같은 파일시스템에서 사용할 수 있는 원자적 잠금이 있다면 편집 전에 획득한다. 로컬 잠금은 다른 세션/호스트까지 보호한다고 주장하지 않는다. 동시 실행 여부를 판정할 수 없으면 `concurrency_unknown`으로 **수정 없이 종료**한다. 타 실행을 강제 종료하거나 잠금을 시간만 보고 빼앗지 않는다.
- Git 변경은 읽은 기준 SHA에 대해 작성한다. 커밋 직전 main이 바뀌었다면 변경 파일 충돌을 확인하고 최신 기준으로 재검토한다. 강제 push 금지. 원격 head와 변경 blob을 검증한다.
- 커밋·업로드·예약 생성 결과가 중간에 끊겼다면 먼저 원격 결과/예약 목록을 조회해 성공 여부를 확인한다. 같은 외부 작업을 무조건 다시 실행하지 않는다.
- 이 규칙과 예약 프롬프트는 협업 절차이며 플랫폼 전체의 강제 mutex나 사용량 제한 해제 감지 서비스가 아니다. 관찰 불가능한 병행 실행에서 무조건 재개를 보장하지 않는다.

## 4. 체크포인트와 완료 증거

`PLAN.md`: 전체 단계, 완료 조건, 현재 우선순위. `WORK_STATE.json`: 현재 요청, 완료/진행/대기, 산출물, 검증, 마지막 정상 체크포인트, 다음 작업. `WORKLOG.md`: 주요 단계와 근거를 누적 기록한다.

실험·빌드 결과에는 입력/소스/설정/모델 버전 또는 해시, 명령, 종료 코드, 결과 파일, 실제 검증 범위를 연결한다. 시작했거나 파일이 존재한다는 이유만으로 완료 처리하지 않는다. `PASS / CONDITIONAL / FAIL / NOT VERIFIED`를 구분한다. 생성 출력은 가능하면 임시 파일로 만든 뒤 검사하고 원자적으로 교체한다. 중단된 출력은 정상 출력과 섞지 않는다.

주요 단계마다 세 파일을 일관되게 갱신하고 Git에 저장한다. 검증된 앞 단계 반복은 변경·실패·구체적 위험 또는 필수 게이트가 있을 때만 한다. 변경 없는 주기 점검은 채팅의 짧은 상태 보고로 충분하다.

## 5. 채팅 로그

작업 중: `단계 ID / 확인된 내용 / 실제 검사와 판정 / 다음 단계`를 간단히 남긴다.

재개 후 종료 보고:
1. 마지막 정상 체크포인트
2. 재개한 단계
3. 중단으로 다시 실행한 작업
4. 검증 중 발견한 이상
5. 완료 단계와 남은 단계
6. 원래 요청의 최종 완료 여부

A/B 무작업 실행은 상태만 짧게 보고한다. 중단·blocked 상태에는 다음 재개 조건을 반드시 남긴다.

## 6. 예약 범위

이 Work에서 6시간마다 확인한다. 먼저 상태 → 중복 → 완료 → 필요할 때만 복구 → 승인된 잔여 계획 순서를 따른다. 별도의 새 프로젝트나 다른 채팅 과업까지 확장하지 않는다. 예약 세부 정보는 `WORK_STATE.json.automation`에 기록한다. 예약 실행 중 새 예약을 만들지 않는다.

현재 SWT 감사에서 제안한 환자 분할 수정·재튜닝·출력 재생성, 글꼴 전면 변경, Loss 화면·탭 통합은 후속 논의 대상이다. 사용자의 이후 지시로 범위가 바뀌면 그 지시와 상태 파일을 갱신한다. 현재 보류 항목을 영구 금지로 해석하지 않는다.
