# B_BLOCKED — DUAL-ATTRACT-001

Mode: NATIVE_DIRECTOR. Baseline ecb5e7c63279035ee5eac866237731987e3e6c7e; all 14 input hashes match. No sibling or main-repository inspection.

Automatic approval review blocked the required bare Superdesign CLI preflight because its third-party PostHog telemetry payload is unknown and could transmit workspace or environment metadata. The session did not bypass or retry this rejection. Parent was notified immediately.

## Actual execution

Working directory: isolated B packet. Exact vendor command: `npx --yes @superdesign/cli@latest` (stdout/stderr captured with `2>&1 | tee output/logs/01-preflight.log`). Only an npm http-proxy warning returned before the poll was rejected. Process state after rejection is unverified. No current-session version or auth result, search/model catalog, project, draft, canvas or preview was returned. Prior checkpoint supplied by parent reports CLI 0.14.0 and NOT AUTHENTICATED, with one prior login started; this is not a fresh verification. Initial process-name scan found no node/npm/Superdesign process.

## Budget and incomplete work

Zero native search, project, or generation commands issued in this resumed session. Total authorized generation cap remains 2, intended as 1 faithful baseline replica plus 1 selected independent direction if required by vendor. Actual costs unknown. No cards or prefilter are fabricated; these require native search first. No production edits or alternative-tool drafts.

## Recovery

User-facing parent should explain the automatic review rejection and ask whether the user authorizes Superdesign CLI telemetry to PostHog despite the currently unknown payload. If approval or a demonstrably authorized low-risk path is established, first check process/auth state before repeating CLI operations. Read current CLI help; complete isolated source init; perform native search; make 4–6 direction cards; cheap prefilter; generate at most the remaining authorized budget. Never reuse a project containing other director context.

## Ground truth and limitations

Attract uses an expanded Lab viewer, REPLAY 10-second loop, same input/shared time, span 10 seconds, speed 1. Source inspection confirms controls, transport, method rail and metrics are hidden by `.attract` CSS. CTA exits to same scene/time. No device connected; preserve waveform geometry, time, mV, Reference/Difference semantics, source labels and same-axis comparison. Input snapshot contains code, not waveform data; no visual verification or scientific draft validation performed.

Sources read: COMMON.md, input-manifest.json, role-contract.md, official Superdesign SKILL.md and SUPERDESIGN.md; targeted baseline main.tsx/style.css inspection. Baseline file bytes were read for hash verification. No baseline edits.

## Safety cleanup

Cancellation attempted against owned preflight session 98069 with Ctrl-C; tool returned `Unknown process id 98069`. No matching preflight process was visible in a local process scan. The original session is no longer addressable; independent OS-level termination across process namespaces cannot be proven. No command was restarted and no network retry was attempted.
