# 이 Work의 중단 복구·예약 실행 규칙

버전 1.2 · 2026-09-16 (KST) · 사용자가 요청한 지속 지침. 수동 작업과 예약 실행에 모두 적용한다.

> 2026-09-16 사용자 결정으로 v1.1의 repository-wide `execution_lock` 방식은 폐기되었다. 변경 이유와 마이그레이션 기록은 `docs/23_concurrency_and_resume_policy_revision.md`를 따른다.

## 1. 시작할 때 읽을 것

현재 채팅의 최신 사용자 지시 → `AGENTS.md` → `WORK_STATE.json` → `PLAN.md`의 현재 상태 → `WORKLOG.md`의 해당 단계 증거를 읽는다. GUI 수정에는 `docs/21_ui_refinement_workflow_final.md`도 적용한다. 최신 사용자 지시가 과거 문서보다 우선하며, 문서의 제안·과거 승인 대기를 현재 작업 승인으로 오해하지 않는다.

지속 저장소는 https://github.com/userpawbaw/ecg-gui-design-review 이다. 로컬 경로와 세션 상태는 환경마다 다를 수 있다. 시작 시 원격 main 및 자신의 작업 branch SHA와 관련 파일을 조회하고 로컬 변경과 비교한다. 로컬이 오래됐다는 이유로 미커밋 사용자 변경을 덮어쓰지 말고 별도로 보존한다. 채팅의 “완료”보다 파일·로그·테스트·원격 커밋을 우선한다. 문서만 바뀌었다면 대용량 데이터 전체를 다시 생성하지 않는다.

## 2. 첫 판단: A / B / C

**A — 동일 자원에 대한 실제 중복 작업이 진행 중:** 세션이나 기기가 열려 있다는 이유만으로 중복이라고 판단하지 않는다. 같은 output path, 같은 Release/tag, 같은 대규모 데이터 생성, 같은 실제 장치/AFE, 같은 배포 대상처럼 실제로 충돌하는 resource가 있는지 확인한다. 동일 resource의 정상 진행 job이 확인되면 그 작업만 중복 실행하지 않는다. 관계없는 문서·UI 설계·별도 branch 코드 작업은 계속할 수 있다.

**B — 원래 승인된 계획 전체 완료:** 계획의 완료 조건과 해당 버전의 산출물·검증 기록을 가볍게 대조한다. 모두 완료면 반복 실행이나 불필요한 파일 변경 없이 상태만 보고한다. 현재 요청 완료와 전체 GUI/장치 프로젝트 완료는 별개다.

**C — 중단·미완료:** 원래 계획, 마지막 검증된 체크포인트, 중단 단계, 실제 변경분, 종료 코드/로그/결과, 미시작 단계를 확인한다. 신뢰할 수 있는 완료 결과는 유지하고 다음 단계부터 이어 간다. 일부 수정·불완전 출력·종료 불명·검증 불가이면 그 단계의 안전한 시작점부터 복구한다. 불완전한 결과에 의존한 후속 단계도 필요한 만큼 검증한다.

미완료지만 필수 자료·권한·사용자 결정·실기 검증이 없으면 `blocked` 또는 `awaiting_user`로 남기고 이유와 재개 조건만 보고한다. 이를 전체 완료로 처리하거나 차단된 실행 경로를 우회하지 않는다. 사용자가 중지·취소·보류한 작업을 예약으로 재개하지 않는다.

## 3. Git/GitHub 기반 협업과 resource-scoped 충돌 제어

### 3.1 repository-wide lock 폐기

`WORK_STATE.json.execution_lock`, owner_id/generation 기반 on/off 소유권, 모든 mutation 전 start-commit을 요구하는 규칙은 더 이상 운영 게이트가 아니다. 과거 기록은 이력으로 보존할 수 있지만 새 작업을 막는 근거로 사용하지 않는다.

`WORK_STATE.json`은 저장소 소유권 표시가 아니라 **상태·체크포인트·재개 정보**를 위한 파일이다.

### 3.2 일반 문서·코드 작업

일반적인 docs/UI/code 작업은 Git의 branch/commit/history를 기본 충돌 제어 수단으로 사용한다.

1. 시작 시 원격 main과 자신의 branch 최신 SHA를 확인한다.
2. 의미 있는 독립 작업은 가능하면 별도 branch에서 수행한다.
3. 변경 전에 관련 파일과 최신 사용자 결정을 읽는다.
4. commit/push/merge 전 원격 변경을 다시 확인한다.
5. 같은 파일 또는 의미적으로 같은 기능이 다른 branch에서 바뀌었으면 diff를 비교해 수동 통합하거나 merge/rebase한다.
6. force push로 다른 변경을 덮어쓰지 않는다.
7. 원격이 변경되었다는 이유만으로 자신의 미커밋 사용자 변경을 삭제하지 않는다.
8. 다른 세션이 존재한다는 사실만으로 관계없는 작업을 멈추지 않는다.

### 3.3 resource-scoped advisory job 상태

실제 중복 비용이나 파괴적 충돌이 있는 작업에만 `WORK_STATE.json.active_jobs` 같은 advisory 상태를 사용할 수 있다.

예:
- 동일한 600초 replay/data/model output 재생성
- 같은 Release asset/tag 생성·업로드
- 같은 deployment target 교체
- 실제 장치/AFE 독점 사용
- 같은 output directory를 파괴적으로 갱신하는 장기 job

각 job은 가능하면 `task_id`, `resource`, `branch`, `started_at`, 실제 runner/job ID, `last_progress_at`, progress evidence를 기록한다.

중요:
- advisory job은 **동일 resource 작업의 중복만 방지**한다.
- unrelated docs/UI/code 작업을 차단하지 않는다.
- stale job 기록 하나가 저장소 전체 block이 되지 않는다.
- 생존 여부가 불명확하면 해당 resource에 대한 파괴적 재실행만 보류하고, 나머지 작업은 계속할 수 있다.

### 3.4 충돌 판단 기준

동시 작업 조정이 필요한지 다음 순서로 본다.

1. 같은 파일을 수정하는가?
2. 같은 기능/상태 계약을 서로 다른 방식으로 수정하는가?
3. 같은 generated artifact/output path를 쓰는가?
4. 같은 Release/tag/deployment/device를 사용하는가?
5. 동일한 비싼 실험·학습·데이터 생성을 반복하는가?

전부 아니면 기본적으로 병렬 작업을 허용한다.

### 3.5 중단 세션의 재개

재개 시 과거 세션의 기억이나 checkout이 최신이라고 가정하지 않는다.

1. 최신 원격 main과 자신의 branch SHA 확인
2. `AGENTS.md`, 이 정책, `WORK_STATE.json`, 관련 계획/로그 읽기
3. 중단 전 base와 최신 원격 diff 확인
4. 미커밋/미푸시 변경 보존
5. 새 문서/설계/commit이 현재 작업과 관련되면 먼저 반영
6. 마지막 검증 checkpoint부터 필요한 부분만 재개

사용량 제한이나 강제 종료로 이전 세션이 사라졌다는 이유만으로 별도 “잠금 해제” 절차는 필요하지 않다.

### 3.6 결과 불명과 외부 작업

커밋·업로드·배포·예약 생성처럼 외부 side effect가 있는 작업이 응답 유실로 성공 여부가 불명확하면 먼저 원격 결과를 조회한다. 같은 외부 작업을 무조건 반복하지 않는다.

## 4. 체크포인트와 완료 증거

`PLAN.md`: 전체 단계, 완료 조건, 현재 우선순위. `WORK_STATE.json`: 현재 요청, 완료/진행/대기, 산출물, 검증, 마지막 정상 체크포인트, 다음 작업. `WORKLOG.md`: 주요 단계와 근거를 누적 기록한다.

실험·빌드 결과에는 입력/소스/설정/모델 버전 또는 해시, 명령, 종료 코드, 결과 파일, 실제 검증 범위를 연결한다. 시작했거나 파일이 존재한다는 이유만으로 완료 처리하지 않는다. `PASS / CONDITIONAL / FAIL / NOT VERIFIED`를 구분한다. 생성 출력은 가능하면 임시 파일로 만든 뒤 검사하고 원자적으로 교체한다. 중단된 출력은 정상 출력과 섞지 않는다.

주요 단계마다 상태·계획·로그를 필요한 만큼 일관되게 갱신하고 Git에 저장한다. 검증된 앞 단계 반복은 변경·실패·구체적 위험 또는 필수 게이트가 있을 때만 한다. 변경 없는 주기 점검은 채팅의 짧은 상태 보고로 충분하다.

## 5. 채팅 로그

작업 중: `단계 ID / 확인된 내용 / 실제 검사와 판정 / 다음 단계`를 간단히 남긴다.

재개 후 종료 보고:
1. 마지막 정상 체크포인트
2. 재개한 단계
3. 중단으로 다시 실행한 작업
4. 검증 중 발견한 이상
5. 완료 단계와 남은 단계
6. 원래 요청의 최종 완료 여부

무작업 실행은 새 정보만 짧게 알린다. 중단·blocked 상태에는 다음 재개 조건을 반드시 남긴다.

## 6. 예약 범위

이 Work에서 6시간마다 확인한다. 먼저 최신 사용자 지시와 원격 상태 → 완료 여부 → 실제 동일 resource 중복 여부 → 필요할 때만 복구 → 승인된 잔여 계획 순서를 따른다. repository-wide on/off lock을 만들거나 해제하지 않는다.

동일 resource에 이미 확인 가능한 장기 job이 있으면 그 job만 중복 실행하지 않는다. 별도의 docs/UI/설계/독립 branch 작업은 충돌하지 않는 한 계속할 수 있다. 예약 실행 중 새 예약을 만들지 않는다.

현재 SWT 감사에서 제안한 환자 분할 수정·재튜닝·출력 재생성, 글꼴 전면 변경, Loss 화면·탭 통합은 후속 논의 대상이다. 사용자의 이후 지시로 범위가 바뀌면 그 지시와 상태 파일을 갱신한다. 현재 보류 항목을 영구 금지로 해석하지 않는다.
