# Independent concept — frozen before previous GUI inspection

Open `index.html` with the sibling `../data/` folder present. No installation, CDN or network is required. Alternatively run `node scripts/preview.cjs` at the repository root and open `http://localhost:8765/independent/index.html` on your own machine.

The selected methods use genuine stored outputs from the pinned source project's demo bank. This is an archive-based GUI prototype, not browser inference or live hardware acquisition. `data/provenance.json` records the source and freshness limitations.

Read `docs/02_independent_design.md` for component roles, data contracts, alternatives and motion decisions. The common review rubric was fixed in `docs/03_acceptance_rubric.md`.

Node numerical checks passed. The cloud browser could not open local file/localhost previews; rendered UI validation was not claimed before freeze.
