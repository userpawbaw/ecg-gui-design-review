{
  "name": "ecg-spike-video-scrub",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Spike: AI-video → scroll animation player (D-023). Frame sequence scrubbed by Lenis scroll, sub-frame blend, motion-normalised remap, pointer gaze (REF-002 constants), optional depth parallax and film post. Test clip is a Blender stand-in, not AI video; all content is demo.",
  "scripts": {
    "clip": "python3 scripts/import_clip.py",
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4193"
  },
  "dependencies": {"lenis": "1.3.26"},
  "devDependencies": {"@playwright/test": "^1.56.0", "typescript": "^5.9.0", "vite": "^7.0.0"}
}
