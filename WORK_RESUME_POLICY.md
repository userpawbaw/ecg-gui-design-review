# 이 Work의 중단 복구·예약 실행 규칙

버전 1.1 · 2026-09-15 (KST) · 사용자가 요청한 지속 지침. 수동 작업과 예약 실행에 모두 적용한다.

## 1. 시작할 때 읽을 것

현재 채팅의 최신 사용자 지시 → `AGENTS.md` → `WORK_STATE.json` → `PLAN.md`의 현재 상태 → `WORKLOG.md`의 해당 단계 증거를 읽는다. GUI 수정에는 `docs/21_ui_refinement_workflow_final.md`도 적용한다. 최신 사용자 지시가 과거 문서보다 우선하며, 문서의 제안·과거 승인 대기를 현재 작업 승인으로 오해하지 않는다.

지속 저장소는 https://github.com/userpawbaw/ecg-gui-design-review 이다. 로컬 경로는 세션마다 바뀔 수 있다. 원격 main의 SHA와 관련 파일을 조회하고 로컬 변경을 비교한다. 로컬이 오래됐다는 이유로 미커밋 사용자 변경을 덮어쓰지 말고 별도로 보존한다. 채팅의 “완료”보다 파일·로그·테스트·원격 커밋을 우선한다. 문서만 바뀌었다면 대용량 데이터 전체를 다시 생성하지 않는다.

## 2. 첫 판단: A / B / C

**A — 다른 실행이 실제 진행 중:** 먼저 §3의 공유 잠금 소유권을 확인하고, 가능한 실행 ID/도구 상태/최근 로그·산출물로 정상 진행 여부를 구분한다. 정상 실행이면 상태만 보고하고 종료한다. 파일 수정, 중복 실험·학습·빌드·테스트, 별도 예약 생성 모두 하지 않는다.

**B — 원래 승인된 계획 전체 완료:** 계획의 완료 조건과 해당 버전의 산출물·검증 기록을 가볍게 대조한다. 모두 완료면 상태만 보고하고 종료한다. 완료 결과를 다시 만들거나 무변경 확인만을 위해 커밋하지 않는다. 현재 요청 완료와 전체 GUI/장치 프로젝트 완료는 별개다.

**C — 중단·미완료:** 원래 계획, 마지막 검증된 체크포인트, 중단 단계, 실제 변경분, 결과·테스트 종료 상태, 미시작 단계를 확인한다. 신뢰할 수 있는 완료 결과는 유지하고 다음 단계부터 이어 간다. 일부 수정·불완전 출력·종료 불명·검증 불가이면 그 단계의 안전한 시작점부터 복구한다. 불완전한 결과에 의존한 후속 단계도 검증하고 필요한 것만 다시 실행한다. 재개한 한 단계에서 멈추지 말고 승인된 남은 작업을 계속 진행한다.

미완료지만 필수 자료·권한·사용자 결정·실기 검증이 없으면 `blocked` 또는 `awaiting_user`로 남기고 이유와 재개 조건만 보고한다. 이를 전체 완료로 처리하거나 차단된 실행 경로를 우회하지 않는다. 사용자가 중지·취소·보류한 작업을 예약으로 재개하지 않는다.

## 3. GitHub 공유 작업 잠금 — 사용자 승인 2026-09-15

사용자 요청: "work status 파일을 github에 두고 진행중인 작업이 없는 경우에만 실행", "작업시작시 on, 끝나면 off". 이 최신 지시는 이전의 모든 세션/호스트 프로세스를 반드시 직접 확인해야 한다는 해석을 대체한다. 참여 실행은 원격 main의 `WORK_STATE.json.execution_lock`을 공통 시작 게이트로 사용한다. 단, 공유 상태가 잘못되었거나 이 규약 밖의 실행 증거가 있으면 추정으로 진행하지 않는다.

### 3.1 로컬/실행/작업 완료의 구분

- ChatGPT 데스크탑 앱이나 GUI 시연 웹페이지가 열려 있는 것만으로 작업 실행 중이 아니다.
- 로컬은 실제 코드 checkout, 수정/빌드/테스트/추론 프로세스가 있는 환경을 뜻한다. 사용자 PC, CLI 환경, Work 원격 환경은 서로 다를 수 있다.
- `execution_lock.active`: 지금 이 저장소의 승인된 수정/실행을 누가 소유하는가.
- `request_status`, 단계 상태: 요청/계획이 완료됐는가. 실행 off여도 승인된 미완료 작업이나 외부 검수 대기가 남을 수 있다.
- on은 예약된 소유권이다. 최근 진행 증거 없이는 "정상 진행 중"이라고 단정하지 않는다. 그러나 생존 확인이 안 된다는 이유로 중복 실행을 시작하지도 않는다.
- off이며 무결한 규약 기록이 있으면 모든 데스크탑을 매번 수동 검사하지 않아도 시작 경쟁에 참여할 수 있다. 이후 소유권 획득을 먼저 확인한다. 직접 파일을 다룰 때 해당 checkout/미커밋 사용자 변경 검사는 여전히 필요하다.
- GitHub 커넥터만 있는 환경에서도 문서/상태의 원격 수정은 가능하다. 파일 실행 도구가 없는 것은 빌드·실험·렌더 검증의 별도 제약이며 동시 실행 상태와 혼동하지 않는다.

### 3.2 상태 필드와 적용 범위

`execution_lock`에는 `protocol_version`, `active`(on/off), `owner_id`, `task_id`, `generation`, `acquired_at`, `last_progress_at`, `released_at`, `last_owner_id`, `phase`, `progress_evidence`, `release_reason`을 둔다. owner_id는 실행마다 새로 만드는 고유 논리 ID이며 플랫폼 실행 ID를 관찰하지 못했으면 그 ID인 것처럼 부르지 않는다. 가능할 때 실제 tool session/job ID는 별도 evidence로 연결한다.

모든 참여 수동/예약 에이전트는 새로 시작할 때 이 규약을 읽는다. GitHub 상태 파일은 앱을 감시하거나 기존 미참여 프로세스를 강제 차단하는 서비스가 아니다. 외부 CLI/다른 Work가 이 규약을 읽지 않고 쓰는 경우까지 보호한다고 주장하지 않는다. 연결 가능한 모든 시작 경로를 참여시켜야 협력적 잠금이 성립한다.

### 3.3 시작 등록: off를 읽었다는 것만으로 실행하지 않는다

1. 최신 main SHA H와 그 H의 상태/정책을 함께 읽는다. active=true이면 수정/빌드/실험/학습을 새로 시작하지 않는다. 정상 진행 증거가 없으면 "소유권 점유, 생존 미확인"으로 보고한다.
2. active=false이고 상태 구조가 유효하면 새로운 owner_id를 만든다. generation을 1 증가시키고 active=true, task_id, 시각, 단계와 근거를 기록한다.
3. H의 tree를 유지한 채 상태/계획/로그 변경을 넣고 **부모가 H 하나인 시작 커밋 C**를 만든다.
4. main을 C로 **fast-forward only (force=false)** 갱신한다. 동일 H에서 시작한 다른 실행의 서로 다른 시작 커밋은 형제 커밋이므로 둘 모두 main이 될 수 없다. 이 절차는 임의의 ref 갱신 전체를 CAS라고 부르는 것이 아니며, 정확히 H를 부모로 고정한 후보에 한정된다.
5. 원격 main의 owner_id/generation/active를 다시 읽고 자신의 등록이 포함됐음을 확인한 실행만 작업한다.
6. 갱신 충돌 시 최신 상태부터 다시 읽는다. 상대가 on이면 종료한다. off여도 최신 변경을 재검토한 뒤 **새 시작 시도**로 처리한다. 실패한 시작 커밋을 자동 merge/rebase해 상대의 on을 덮지 않는다.
7. 응답 유실/성공 불명이면 main과 owner_id 및 커밋 이력부터 조회한다. 확인 전 실제 작업이나 다른 owner_id로 재시도하지 않는다. 등록 결과를 끝내 확인할 수 없으면 concurrency_unknown으로 종료한다.

근거: [GitHub Update a reference](https://docs.github.com/en/rest/git/refs#update-a-reference)는 force=false의 fast-forward 갱신을 제공한다. 분산 동시 실행 경합 자체를 실제로 재현·통과한 것으로 기록하지 않는다.

### 3.4 진행, 종료와 대기

- 장기 명령 시작 전과 각 주요 체크포인트/원격 쓰기 전에 현재 owner_id/generation을 확인한다. 잃었거나 읽기 실패하면 후속 작업을 시작하지 않는다. 실행 중 관리 가능한 자식 job은 안전하게 중지/확인하고, 종료 불명인 job이 있으면 잠금을 해제하지 않는다.
- 주요 단계에 기존 PLAN/WORK_STATE/WORKLOG 커밋과 함께 last_progress_at 및 증거를 갱신한다. 사소한 heartbeat만을 위한 반복 커밋은 만들지 않는다.
- 정상 완료 또는 안전한 blocked/awaiting_user 전환 시, 자신의 자식 명령이 종료됐고 산출물이 체크포인트에 남았음을 확인한다. 같은 owner_id/generation일 때만 active=false, owner_id=null, last_owner_id, released_at, release_reason을 기록해 커밋하고 원격을 확인한다.
- 마지막 산출물/검증 기록과 off 전환을 한 커밋에 묶을 수 있다. off 이후에는 읽기 검증만 한다. 추가 수정은 새로 잠금을 얻는다.
- request_status=blocked/completed와 잠금 off는 함께 성립할 수 있다. off를 전체 작업 완료로 해석하지 않는다.
- 작업 없는 상태 점검은 잠금 획득/해제 커밋이 필요 없다.

### 3.5 비정상 중단과 자동 복구의 한계

- 세션 강제 종료/사용량 제한/네트워크 장애 시 finally나 off 커밋이 실행되지 않을 수 있다. on이 오래됐다는 이유, 짧은 PID 조회, 최근 로그 부재만으로 off로 바꾸거나 잠금을 빼앗지 않는다. v1에는 시간 만료 자동 해제가 없다.
- 관리 가능한 runner/job의 최종 종료 상태와 자식 작업 종료를 확인할 수 있다면, recovery owner는 그 근거와 이전 owner_id/generation을 기록하고 H 고정/force=false 시작 절차로 잠금을 인계받을 수 있다. 필요할 때 사용자에게 해당 이전 실행이 종료됐다는 확인을 받아 복구하는 것도 가능하다.
- 종료를 증명할 자료가 없으면 concurrency_unknown으로 수정 없이 종료한다. 정상적인 off 상태에서는 이런 수동 확인을 반복 요구하지 않는다.
- 이전 세션이 되살아나면 기존 owner_id가 아직 자신의 것인지 먼저 확인한다. 소유권을 잃은 세션은 수정/업로드/후속 명령을 중지한다.
- 현재 제공된 도구만으로 플랫폼 세션 전체를 조회하거나 종료 hook/상주 감시기를 설치하지 않았다. 따라서 "규약 참여 에이전트가 수행하는 시작·끝 등록"과 "강제 중단도 자동 감지하는 실행 관리자"를 구분한다.

### 3.6 최초 전환의 근거

이 규약은 사용자의 위 최신 요청, 사용자 PC에서 명령을 실행 중이지 않다는 설명, 원격 기존 execution=finished, 아직 새로운 실행 잠금이 없다는 상태를 근거로 도입한다. 이를 다른 모든 호스트의 프로세스 검사 결과로 기록하지 않는다. 최초 시작도 읽은 main의 단일 부모 커밋과 force=false로 보호한다. 새 규약 도입 후 실제 자동 예약의 획득/해제 및 강제중단 복구는 별도 관찰 대상이다.

## 4. 체크포인트와 완료 증거

`PLAN.md`: 전체 단계, 완료 조건, 현재 우선순위. `WORK_STATE.json`: 현재 요청, 완료/진행/대기, 산출물, 검증, 마지막 정상 체크포인트, 다음 작업. `WORKLOG.md`: 주요 단계와 근거를 누적 기록한다.

실험·빌드 결과에는 입력/소스/설정/모델 버전 또는 해시, 명령, 종료 코드, 결과 파일, 실제 검증 범위를 연결한다. 시작했거나 파일이 존재한다는 이유만으로 완료 처리하지 않는다. `PASS / CONDITIONAL / FAIL / NOT VERIFIED`를 구분한다. 생성 출력은 가능하면 임시 파일로 만든 뒤 검사하고 원자적으로 교체한다. 중단된 출력은 정상 출력과 섞지 않는다.

주요 단계마다 세 파일을 일관되게 갱신하고 Git에 저장한다. 검증된 앞 단계 반복은 변경·실패·구체적 위험 또는 필수 게이트가 있을 때만 한다. 변경 없는 주기 점검은 채팅의 짧은 상태 보고로 충분하다.

## 5. 채팅 로그

작업 중: `단계 ID / 확인된 내용 / 실제 검사와 판정 / 다음 단계`를 간단히 남긴다.

재개 후 종료 보고:
1. 마지막 정상 체크포인트
2. 재개한 단계
3. 중단으로 다시 실행한 작업
4. 검증 중 발견한 이상
5. 완료 단계와 남은 단계
6. 원래 요청의 최종 완료 여부

A/B 무작업 실행은 상태만 짧게 보고한다. 중단·blocked 상태에는 다음 재개 조건을 반드시 남긴다.

## 6. 예약 범위

이 Work에서 6시간마다 확인한다. 먼저 상태 → 중복 → 완료 → 필요할 때만 복구 → 승인된 잔여 계획 순서를 따른다. 별도의 새 프로젝트나 다른 채팅 과업까지 확장하지 않는다. 예약 세부 정보는 `WORK_STATE.json.automation`에 기록한다. 예약 실행 중 새 예약을 만들지 않는다.

현재 SWT 감사에서 제안한 환자 분할 수정·재튜닝·출력 재생성, 글꼴 전면 변경, Loss 화면·탭 통합은 후속 논의 대상이다. 사용자의 이후 지시로 범위가 바뀌면 그 지시와 상태 파일을 갱신한다. 현재 보류 항목을 영구 금지로 해석하지 않는다.
