---
name: dual-creative-director
description: Orchestrate independent reference and Superdesign directors for significant new HIGH-zone ECG Expo design directions, or explicit dual-director requests. Route MEDIUM work through A first and skip automatic creative exploration for LOW or frozen implementation.
---

Read `docs/uiux_system/16_DUAL_CREATIVE_DIRECTOR.md` as the common contract. Use `00_UIUX_MASTER.md` and `05_TOOL_SKILL_ROUTING.md` to classify the request; an explicit A-only, B-only or Concretizer request overrides automatic Dual routing.

Freeze a shared baseline/context manifest. Run A with `13_REFERENCE_GROUNDED_CREATIVE_MINING.md` and B with `14_SUPERDESIGN_GENERATION_LAYER.md` NATIVE_DIRECTOR in separate clean contexts, without inheriting the orchestrator conversation. Pass only baseline/goal/constraints and the relevant role instructions. Keep output paths and B tool state separate. Do not share A references/candidates or B search/cards/drafts until both passes are frozen. Use fresh sessions/handoffs if isolation is unavailable; do not call role-switching in one conversation independent execution.

A returns 5–8 reference-grounded concepts. B returns 4–6 Direction Cards, a cheap prefilter with reasons, then 1–2 drafts at most. Compare duplicates within B only before cross-review. If every candidate breaks hard constraints, return zero drafts and explain. Never spend extra credits merely to fill a quota.

Without shell, execute A and produce an A-free B handoff per `06_CHAT_WORK_CODEX_HANDOFF.md`. With shell, check actual vendor/auth/network and context isolation. A B handoff or failure is not completed Dual execution.

After both independent outputs freeze, cross-review, then optionally one complementary Hybrid through CONCRETIZER within authorized budget. Hand results to user alignment and a separate validator; do not implement automatically. Return provenance, status, budget use, rejected candidates and limitations per §8; record significant decisions under `10_RECORD_KEEPING.md`.
